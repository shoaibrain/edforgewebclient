{
  "name": "@edforge/shared-types",
  "version": "1.0.0",
  "description": "Shared TypeScript types for EdForge monorepo",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "npx tsc",
    "watch": "npx tsc --watch",
    "clean": "rimraf dist",
    "validate:sync": "ts-node --project scripts/tsconfig.json scripts/validate-sync.ts"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "rimraf": "^5.0.5",
    "ts-node": "^10.9.2",
    "@types/node": "^20.0.0"
  },
  "private": true
}

